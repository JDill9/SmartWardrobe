# SmartWardrobe Architecture Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        UI Layer (Your Team)                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Login      │  │  Wardrobe    │  │   Outfit     │      │
│  │   Screen     │  │   List       │  │   Creator    │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
└─────────┼──────────────────┼──────────────────┼─────────────┘
          │                  │                  │
          └──────────────────┼──────────────────┘
                             │
┌────────────────────────────┼─────────────────────────────────┐
│              Repository Layer (✅ COMPLETE)                   │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────┐ │
│  │  AuthRepository │  │ WardrobeRepo    │  │ OutfitRepo  │ │
│  │  - register()   │  │ - addItem()     │  │ - create()  │ │
│  │  - signIn()     │  │ - getAll()      │  │ - getAll()  │ │
│  │  - signOut()    │  │ - update()      │  │ - update()  │ │
│  │  - resetPwd()   │  │ - delete()      │  │ - delete()  │ │
│  └────────┬────────┘  └────────┬────────┘  └──────┬──────┘ │
└───────────┼──────────────────────┼───────────────────┼───────┘
            │                      │                   │
            └──────────────────────┼───────────────────┘
                                   │
┌──────────────────────────────────┼───────────────────────────┐
│               Data Models (✅ COMPLETE)                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │     User     │  │ WardrobeItem │  │    Outfit    │      │
│  │ - uid        │  │ - id         │  │ - id         │      │
│  │ - email      │  │ - name       │  │ - name       │      │
│  │ - name       │  │ - category   │  │ - itemIds[]  │      │
│  │ - photoUrl   │  │ - color      │  │ - season     │      │
│  └──────────────┘  │ - imageUrl   │  │ - favorite   │      │
│                    │ - ai3DModel  │  └──────────────┘      │
│                    └──────────────┘                         │
└────────────────────────────┬───────────────────────────────┘
                             │
┌────────────────────────────┼───────────────────────────────┐
│                   Firebase Backend                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │   Firebase   │  │  Firestore  │  │   Storage   │        │
│  │    Auth      │  │   Database  │  │  (Images)   │        │
│  └─────────────┘  └─────────────┘  └─────────────┘        │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow Examples

### Example 1: User Registration Flow
```
User Input (UI)
    │
    ├─> AuthRepository.registerUser(email, password, name)
    │       │
    │       ├─> Firebase Authentication (create account)
    │       │
    │       ├─> Firestore Database (create user document)
    │       │
    │       └─> Return Result.Success(User)
    │
    └─> UI shows success / navigates to main screen
```

### Example 2: Add Wardrobe Item Flow
```
User uploads photo (UI)
    │
    ├─> Upload to Firebase Storage
    │       │
    │       └─> Get imageUrl
    │
    ├─> (Future) AI processes image → ai3DModelUrl
    │
    ├─> WardrobeRepository.addWardrobeItem(item)
    │       │
    │       ├─> Firestore adds document to wardrobeItems collection
    │       │
    │       └─> Return Result.Success(WardrobeItem with ID)
    │
    └─> UI shows item in wardrobe list
```

### Example 3: Real-time Wardrobe Updates
```
UI subscribes to Flow
    │
    ├─> WardrobeRepository.getAllWardrobeItemsFlow()
    │       │
    │       └─> Firestore Snapshot Listener
    │               │
    │               ├─> Emits Result.Success(items) whenever data changes
    │               │
    │               └─> UI automatically updates
    │
    └─> User sees live updates (even from other devices!)
```

### Example 4: Create Outfit Flow
```
User selects wardrobe items (UI)
    │
    ├─> OutfitRepository.createOutfit(outfit)
    │       │
    │       ├─> Validates all itemIds exist
    │       │
    │       ├─> Firestore adds document to outfits collection
    │       │
    │       └─> Return Result.Success(Outfit with ID)
    │
    └─> UI shows outfit in outfits list
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      Security Layers                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Layer 1: Firebase Authentication                          │
│  ├─> All API calls require valid auth token                │
│  └─> Token automatically included by Firebase SDK          │
│                                                             │
│  Layer 2: Firestore Security Rules                         │
│  ├─> Users can only read their own data                    │
│  ├─> Users can only write data with their userId           │
│  └─> Rules enforced at database level                      │
│                                                             │
│  Layer 3: Repository Ownership Checks                      │
│  ├─> Verify userId before operations                       │
│  ├─> Prevent unauthorized access attempts                  │
│  └─> Return meaningful error messages                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🗄️ Database Schema

### Firestore Collections

```
firestore/
│
├─ users/
│  └─ {userId} (document)
│     ├─ uid: String
│     ├─ email: String
│     ├─ displayName: String
│     ├─ photoUrl: String (optional)
│     ├─ createdAt: Timestamp
│     └─ updatedAt: Timestamp
│
├─ wardrobeItems/
│  └─ {itemId} (document)
│     ├─ id: String
│     ├─ userId: String ◄─────────┐ (indexed for queries)
│     ├─ name: String             │
│     ├─ category: String ◄────┐  │ (indexed for filtering)
│     ├─ color: String         │  │
│     ├─ brand: String         │  │
│     ├─ size: String          │  │
│     ├─ imageUrl: String      │  │
│     ├─ ai3DModelUrl: String  │  │ (for future 3D integration)
│     ├─ tags: [String]        │  │
│     ├─ createdAt: Timestamp  │  │
│     └─ updatedAt: Timestamp  │  │
│                               │  │
└─ outfits/                    │  │
   └─ {outfitId} (document)    │  │
      ├─ id: String             │  │
      ├─ userId: String ◄───────┼──┘ (indexed for queries)
      ├─ name: String           │
      ├─ itemIds: [String] ◄────┘ (references to wardrobeItems)
      ├─ occasion: [String]
      ├─ season: String
      ├─ isFavorite: Boolean ◄──── (indexed for favorites query)
      ├─ createdAt: Timestamp
      └─ updatedAt: Timestamp
```

### Firestore Indexes (Created Automatically)

```
wardrobeItems:
  - userId (ascending)
  - category (ascending)
  - createdAt (descending)

outfits:
  - userId (ascending)
  - isFavorite (ascending)
  - createdAt (descending)

Array-contains index:
  - outfits.itemIds (for finding outfits containing specific items)
```

## 🔄 State Management Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     UI State Patterns                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Pattern 1: One-time Fetch                                 │
│  ┌──────────────────────────────────────────────────┐     │
│  │ User opens screen                                 │     │
│  │    ↓                                              │     │
│  │ Launch coroutine                                  │     │
│  │    ↓                                              │     │
│  │ Call repository.getSomething()                    │     │
│  │    ↓                                              │     │
│  │ when (result) {                                   │     │
│  │   Success -> Update UI state                      │     │
│  │   Error -> Show error message                     │     │
│  │ }                                                 │     │
│  └──────────────────────────────────────────────────┘     │
│                                                             │
│  Pattern 2: Real-time Updates (Flow)                       │
│  ┌──────────────────────────────────────────────────┐     │
│  │ User opens screen                                 │     │
│  │    ↓                                              │     │
│  │ LaunchedEffect subscribes to Flow                 │     │
│  │    ↓                                              │     │
│  │ repository.getSomethingFlow().collect {           │     │
│  │   when (result) {                                 │     │
│  │     Success -> UI updates automatically           │     │
│  │     Error -> Show error                           │     │
│  │   }                                               │     │
│  │ }                                                 │     │
│  │    ↓                                              │     │
│  │ Database changes → UI updates in real-time!       │     │
│  └──────────────────────────────────────────────────┘     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 Integration Points for 3D/AI Team

```
┌─────────────────────────────────────────────────────────────┐
│              3D Wardrobe Visualization Pipeline             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Step 1: Photo Capture                                     │
│  ┌────────────────────────────────────────┐               │
│  │ User takes photo with camera           │               │
│  │    ↓                                   │               │
│  │ Upload to Firebase Storage             │               │
│  │    ↓                                   │               │
│  │ Get permanent URL                      │               │
│  └────────────────────────────────────────┘               │
│                                                             │
│  Step 2: AI Processing (Your Team)                         │
│  ┌────────────────────────────────────────┐               │
│  │ Send imageUrl to AI service            │               │
│  │    ↓                                   │               │
│  │ AI analyzes clothing item              │               │
│  │    ↓                                   │               │
│  │ Generate 3D model with Three.js        │               │
│  │    ↓                                   │               │
│  │ Upload 3D model to storage             │               │
│  │    ↓                                   │               │
│  │ Get ai3DModelUrl                       │               │
│  └────────────────────────────────────────┘               │
│                                                             │
│  Step 3: Save to Database (Already Built!)                 │
│  ┌────────────────────────────────────────┐               │
│  │ val item = WardrobeItem(               │               │
│  │   imageUrl = imageUrl,                 │               │
│  │   ai3DModelUrl = ai3DModelUrl,  ◄─┐   │               │
│  │   ...                              │   │               │
│  │ )                                  │   │               │
│  │ wardrobeRepo.addWardrobeItem(item) │   │               │
│  └────────────────────────────────────┼───┘               │
│                                        │                   │
│  Step 4: 3D Visualization                                  │
│  ┌────────────────────────────────────┼───┐               │
│  │ wardrobeRepo.getAllItemsFlow() ◄───┘   │               │
│  │    ↓                                   │               │
│  │ items.forEach { item ->                │               │
│  │   load3DModel(item.ai3DModelUrl)      │               │
│  │   display in Three.js scene           │               │
│  │ }                                      │               │
│  └────────────────────────────────────────┘               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 📱 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    Production Setup                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Android App (on user device)                              │
│       ↓                                                     │
│  Firebase SDK (local)                                      │
│       ↓                                                     │
│  ══════════════ Internet ══════════════                    │
│       ↓                                                     │
│  Firebase Project (Google Cloud)                           │
│  ├─ Authentication Service                                 │
│  ├─ Firestore Database (NoSQL)                            │
│  ├─ Cloud Storage (Images/3D models)                      │
│  ├─ (Optional) Cloud Functions (serverless)               │
│  └─ (Optional) Firebase Analytics                         │
│                                                             │
│  Scaling: Automatic (handles millions of users)            │
│  Cost: Free tier → Pay as you grow                        │
│  Backup: Automatic daily backups                           │
│  Security: Managed by Google                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🔧 Development Workflow

```
┌─────────────────────────────────────────────────────────────┐
│              Team Development Workflow                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Backend Developer (You) ✅ COMPLETE                        │
│  ├─ Setup Firebase project                                 │
│  ├─ Configure authentication                               │
│  ├─ Design database schema                                 │
│  ├─ Implement repositories                                 │
│  ├─ Write tests                                            │
│  └─ Document usage                                         │
│       │                                                     │
│       ├──────────────────────────┐                         │
│       │                          │                         │
│       ↓                          ↓                         │
│  UI Developer             3D/AI Developer                  │
│  ├─ Build screens         ├─ Photo capture                │
│  ├─ Call repositories     ├─ AI integration                │
│  ├─ Handle user input     ├─ 3D model generation          │
│  └─ Display data          └─ Three.js visualization        │
│                                                             │
│  All teams work in parallel! ✅                             │
│  Database layer is independent of UI/3D                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Performance Characteristics

```
┌─────────────────────────────────────────────────────────────┐
│                    Performance Metrics                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Operation              │ Speed      │ Scale               │
│  ─────────────────────────────────────────────────         │
│  User login            │ < 1 sec    │ Unlimited users      │
│  Add wardrobe item     │ < 500ms    │ Thousands per user   │
│  Fetch all items       │ < 1 sec    │ Real-time updates    │
│  Search items          │ < 500ms    │ Full-text search     │
│  Create outfit         │ < 500ms    │ Complex queries      │
│                                                             │
│  Database reads:  50k/day FREE, then $0.06 per 100k        │
│  Database writes: 20k/day FREE, then $0.18 per 100k        │
│  Storage: 5GB FREE, then $0.026/GB/month                   │
│                                                             │
│  Estimated costs for 1000 active users: < $10/month        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

**Visual Guide Version**: 1.0  
**Last Updated**: November 12, 2025  
**Architecture Status**: ✅ Production Ready
